import { defineComponent } from "vue";

export default defineComponent({
  name: "MInput",
  setup() {
    return () => (
      <>
        <div>JSX MInput:</div>
        <input></input>
      </>
    );
  },
});
